<%= simple_form_for_contentr_paragraph do |f| %>
  <div class='row-fluid'>
    <% @paragraph.class.form_fields.each do |ff| %>
      <% label = @paragraph.class.human_attribute_name(ff[:name]) %>
      <% if ff[:name] == :gallery %>
        <%= f.input ff[:name], collection: Gallery.all.map(&:title), default: @paragraph.gallery || 1, :label => label, :label_html => { :style => "float:none; text-align:left" }, input_html: {class: 'span6'} %>
        <% next %>
      <% end -%>
      <%= f.input ff[:name], as: ff[:typ], :label => label, :label_html => { :style => "float:none; text-align:left" }, input_html: {class: 'span6'} %>
    <% end %>
    <div class='well centered'>
      <%= f.submit "#{btn_text}", class: 'btn btn-inverse btn-small' %>
    </div>
  </div>
<% end %>
